{
  "name": "signa-sign-translator",
  "version": "1.0.0",
  "private": true,
  "description": "SIGNA — real-time, client-side sign language fingerspelling translator using MediaPipe Hands and a geometric heuristic classifier. 100% local, no server, no data leaves the browser.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
